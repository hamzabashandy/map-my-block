export function MapCanvas() {
  return (
    <div
      className="absolute inset-0 flex items-center justify-center"
      style={{ backgroundColor: "#1a1d22" }}
      aria-label="Map placeholder"
    >
      <span className="text-sm text-white/30">Map will load here</span>
    </div>
  );
}
